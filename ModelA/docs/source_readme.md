# ModelA Source Notes

ModelA began as a graph-based alternative to earlier Coulomb-style approaches. It is retained as an intermediate branch rather than as the primary model path.

Key ideas:

- graph representation over global Coulomb descriptors,
- angle encoding rather than amplitude encoding for this branch,
- selected artifacts rather than a complete run tree.
