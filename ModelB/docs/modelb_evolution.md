# ModelB Evolution

ModelB is the retained mainline because it best captures the project's later emphasis on modularity, graph realism, and leakage-aware training.

The retained source is paired with a docs-only `v4_planned/` subtree that records a future redesign direction without presenting that redesign as implemented.
